diff --git a/node_modules/@lanonasis/security-sdk/dist/hash-utils.cjs b/node_modules/@lanonasis/security-sdk/dist/hash-utils.cjs
new file mode 100644
index 0000000..4fb63b9
--- /dev/null
+++ b/node_modules/@lanonasis/security-sdk/dist/hash-utils.cjs
@@ -0,0 +1,67 @@
+"use strict";
+const crypto = require("crypto");
+
+function isSha256Hash(value) {
+  return typeof value === "string" && /^[a-f0-9]{64}$/i.test(value.trim());
+}
+
+function hashApiKey(apiKey) {
+  if (!apiKey || typeof apiKey !== "string") {
+    throw new Error("API key must be a non-empty string");
+  }
+  return crypto.createHash("sha256").update(apiKey).digest("hex");
+}
+
+async function hashApiKeyBrowser(apiKey) {
+  if (!apiKey || typeof apiKey !== "string") {
+    throw new Error("API key must be a non-empty string");
+  }
+  const subtle = (globalThis == null ? void 0 : globalThis.crypto)?.subtle || crypto.webcrypto?.subtle;
+  if (!subtle) {
+    return hashApiKey(apiKey);
+  }
+  const data = new TextEncoder().encode(apiKey);
+  const hashBuffer = await subtle.digest("SHA-256", data);
+  const hashArray = Array.from(new Uint8Array(hashBuffer));
+  return hashArray.map((b) => b.toString(16).padStart(2, "0")).join("");
+}
+
+function ensureApiKeyHash(apiKey) {
+  if (isSha256Hash(apiKey)) {
+    return apiKey.toLowerCase();
+  }
+  return hashApiKey(apiKey);
+}
+
+async function ensureApiKeyHashBrowser(apiKey) {
+  if (isSha256Hash(apiKey)) {
+    return apiKey.toLowerCase();
+  }
+  return hashApiKeyBrowser(apiKey);
+}
+
+function verifyApiKey(apiKey, storedHash) {
+  const computedHash = hashApiKey(apiKey);
+  if (computedHash.length !== storedHash.length) {
+    return false;
+  }
+  const computedBuffer = Buffer.from(computedHash, "hex");
+  const storedBuffer = Buffer.from(storedHash, "hex");
+  return crypto.timingSafeEqual(computedBuffer, storedBuffer);
+}
+
+function generateApiKey() {
+  const randomBytes = crypto.randomBytes(36);
+  const randomString = randomBytes.toString("base64url");
+  return `lns_${randomString}`;
+}
+
+module.exports = {
+  isSha256Hash,
+  hashApiKey,
+  hashApiKeyBrowser,
+  ensureApiKeyHash,
+  ensureApiKeyHashBrowser,
+  verifyApiKey,
+  generateApiKey
+};
diff --git a/node_modules/@lanonasis/security-sdk/dist/hash-utils.d.cts b/node_modules/@lanonasis/security-sdk/dist/hash-utils.d.cts
new file mode 100644
index 0000000..d5c9781
--- /dev/null
+++ b/node_modules/@lanonasis/security-sdk/dist/hash-utils.d.cts
@@ -0,0 +1,9 @@
+export declare function isSha256Hash(value: string): boolean;
+export declare function hashApiKey(apiKey: string): string;
+export declare function hashApiKeyBrowser(apiKey: string): Promise<string>;
+export declare function ensureApiKeyHash(apiKey: string): string;
+export declare function ensureApiKeyHashBrowser(apiKey: string): Promise<string>;
+export declare function verifyApiKey(apiKey: string, storedHash: string): boolean;
+export declare function generateApiKey(): string;
+export type ApiKeyHash = string;
+export type ApiKey = string;
diff --git a/node_modules/@lanonasis/security-sdk/dist/hash-utils.d.ts b/node_modules/@lanonasis/security-sdk/dist/hash-utils.d.ts
new file mode 100644
index 0000000..d5c9781
--- /dev/null
+++ b/node_modules/@lanonasis/security-sdk/dist/hash-utils.d.ts
@@ -0,0 +1,9 @@
+export declare function isSha256Hash(value: string): boolean;
+export declare function hashApiKey(apiKey: string): string;
+export declare function hashApiKeyBrowser(apiKey: string): Promise<string>;
+export declare function ensureApiKeyHash(apiKey: string): string;
+export declare function ensureApiKeyHashBrowser(apiKey: string): Promise<string>;
+export declare function verifyApiKey(apiKey: string, storedHash: string): boolean;
+export declare function generateApiKey(): string;
+export type ApiKeyHash = string;
+export type ApiKey = string;
diff --git a/node_modules/@lanonasis/security-sdk/dist/hash-utils.js b/node_modules/@lanonasis/security-sdk/dist/hash-utils.js
new file mode 100644
index 0000000..e728e90
--- /dev/null
+++ b/node_modules/@lanonasis/security-sdk/dist/hash-utils.js
@@ -0,0 +1,56 @@
+import crypto from "crypto";
+
+export function isSha256Hash(value) {
+  return typeof value === "string" && /^[a-f0-9]{64}$/i.test(value.trim());
+}
+
+export function hashApiKey(apiKey) {
+  if (!apiKey || typeof apiKey !== "string") {
+    throw new Error("API key must be a non-empty string");
+  }
+  return crypto.createHash("sha256").update(apiKey).digest("hex");
+}
+
+export async function hashApiKeyBrowser(apiKey) {
+  if (!apiKey || typeof apiKey !== "string") {
+    throw new Error("API key must be a non-empty string");
+  }
+  const subtle = (globalThis == null ? void 0 : globalThis.crypto)?.subtle || crypto.webcrypto?.subtle;
+  if (!subtle) {
+    return hashApiKey(apiKey);
+  }
+  const data = new TextEncoder().encode(apiKey);
+  const hashBuffer = await subtle.digest("SHA-256", data);
+  const hashArray = Array.from(new Uint8Array(hashBuffer));
+  return hashArray.map((b) => b.toString(16).padStart(2, "0")).join("");
+}
+
+export function ensureApiKeyHash(apiKey) {
+  if (isSha256Hash(apiKey)) {
+    return apiKey.toLowerCase();
+  }
+  return hashApiKey(apiKey);
+}
+
+export async function ensureApiKeyHashBrowser(apiKey) {
+  if (isSha256Hash(apiKey)) {
+    return apiKey.toLowerCase();
+  }
+  return hashApiKeyBrowser(apiKey);
+}
+
+export function verifyApiKey(apiKey, storedHash) {
+  const computedHash = hashApiKey(apiKey);
+  if (computedHash.length !== storedHash.length) {
+    return false;
+  }
+  const computedBuffer = Buffer.from(computedHash, "hex");
+  const storedBuffer = Buffer.from(storedHash, "hex");
+  return crypto.timingSafeEqual(computedBuffer, storedBuffer);
+}
+
+export function generateApiKey() {
+  const randomBytes = crypto.randomBytes(36);
+  const randomString = randomBytes.toString("base64url");
+  return `lns_${randomString}`;
+}

diff --git a/node_modules/@lanonasis/security-sdk/package.json b/node_modules/@lanonasis/security-sdk/package.json
index 38f5b20..3acaa2e 100644
--- a/node_modules/@lanonasis/security-sdk/package.json
+++ b/node_modules/@lanonasis/security-sdk/package.json
@@ -18,6 +18,11 @@
-    ".": {
-      "types": "./dist/index.d.ts",
-      "import": "./dist/index.js",
-      "require": "./dist/index.cjs"
-    }
+    ".": {
+      "types": "./dist/index.d.ts",
+      "import": "./dist/index.js",
+      "require": "./dist/index.cjs"
+    },
+    "./hash-utils": {
+      "types": "./dist/hash-utils.d.ts",
+      "import": "./dist/hash-utils.js",
+      "require": "./dist/hash-utils.cjs"
+    }
	 },
	 "scripts": {
