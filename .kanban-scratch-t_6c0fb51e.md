# Scratch capture — t_6c0fb51e real failure output

Captured 2026-09-18 by kanban worker coda (task t_6c0fb51e).

## Environment prerequisite

The `lanonasis-maas:test` nx target's `runCommand` is
`bun run test -- undefined`. On a host without `~/.bun/bin` on PATH,
the target fails with `/bin/sh: bun: command not found` (exit 1 from nx,
127 from /bin/sh). Verbatim nx output for that case was captured in
the task workspace at `env-no-bun-failure.txt`. **Environment-level fix:
install bun (this host had it missing at session start).**

## `nx run lanonasis-maas:test` (root test target for the lanonasis-maas package)

Exit 0, 149/149 tests pass on PR #152 tip (`e02509f8`).

```
 RUN  v4.1.11 /Users/seyederick/DevOps/projects/active/lan-onasis-monorepo/apps/lanonasis-maas
 Test Files  12 passed (12)
      Tests  149 passed (149)
   Duration  540–578 ms

 NX   Successfully ran target test for project lanonasis-maas
```

**Verdict: GREEN.** The task body warning about "src/server.ts is
standalone-only; production bypasses it" does NOT bite here — the
conformance suite (`tests/conformance/*.test.ts`) mounts an isolated
mock Express app from `tests/utils/test-api-client.ts` and never
imports `src/server.ts`. No test currently asserts prod routing through
`src/server.ts`. Vitest config also excludes `src/server.ts` from
coverage (`vitest.config.ts:42`).

## `nx run @lanonasis/cli-app:test` (the sibling target the parent unit actually needs)

Exit 1, 2 failures, both in `apps/lanonasis-maas/cli/test/load.test.ts`:

```
 FAIL  test/load.test.ts (77.472 s)
  ● Load Testing Suite - In-process CLI Commands › Memory command validation load ›
    handles 40 memory list calls (expected failures without auth)

    expect(received).toBeGreaterThan(expected)
    Expected: > 3.2
    Received:   0

    > 247 |       expect(authFailures.length).toBeGreaterThan(totalRequests * 0.8);

  ● Load Test - MCP Commands › handles 15 concurrent mcp tools commands
    thrown: "Exceeded timeout of 30000 ms for a test."

Test Suites: 1 failed, 2 skipped, 28 passed, 29 of 31 total
Tests:       2 failed, 40 skipped, 407 passed, 449 total
Time:        78.305 s
```

**Verdict: RED.** This is the actual regression bleeding the nx run-many
sweep referenced in the parent unit t_30ba7624 ("10-test regression
bleeding the whole monorepo nx run-many sweep" — note: on this branch
tip the load.test.ts failures are 2, not 10; PR #152's "make cli build
nx-safe" landed, but the load-test fix did not).

## Plain-language one-paragraph summary

The nx target the task body literally names — `lanonasis-maas:test` —
runs 149 vitest tests against memory-service internals (auth
middleware, rate limiting, request validation, mocked OpenAI / Supabase
clients) and all 149 pass on PR #152 tip; the conformance suite uses
an isolated mock Express app rather than the real `src/server.ts`, so
the "production bypasses src/server.ts" warning does not produce any
failing assertion here. The actually-failing target on this branch is
`@lanonasis/cli-app:test`, which jest-runs the CLI's load suite and
fails two of its cases: "handles 40 memory list calls" because zero
requests return auth failures (the CLI short-circuits before the auth
layer when no token env is set, so the test's expectation that 80% of
unauth calls fail is unmet), and "handles 15 concurrent mcp tools
commands" because the CLI exceeds jest's 30 s default timeout while
servicing 15 concurrent MCP commands — so the implementer card
t_58ec0db5 needs to focus on `cli/test/load.test.ts`, not on
`apps/lanonasis-maas` root, and the implementer should not 'fix' any
test by routing it through `src/server.ts` (which production does not
use). An environment prerequisite also surfaced: `bun` was not installed
on this host at session start, and the nx target silently exits with
`/bin/sh: bun: command not found` rather than producing any vitest
output — so any CI runner that lacks bun will appear to fail
`lanonasis-maas:test` for a non-code reason that should be flagged
before being treated as a regression.
