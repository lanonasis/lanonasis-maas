# Node modules
node_modules/

# Environment variables - NEVER commit real API keys
.env
.env.*
.env.local
.env.development
.env.production
.env.test
!.env.example
!.env.template

# Build outputs
dist/
build/
out/
*.vsix

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/
.nyc_output/

# TestSprite — keep generated reports/test code, ignore transient run artifacts
testsprite_tests/tmp/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Local Netlify folder
.netlify

# Temporary files
*.tmp
*.temp
.cache/

# Security
*.pem
*.key
*.cert
*.p12

# Scripts with hardcoded credentials
**/quick-auth-fix.sh
**/emergency-*.sh
**/*-fix.sh
secrets/

# Local configuration
*.local
config.local.json

# Security - Prevent secret commits
.env
.env.local
.env.*.local
*.key
*.pem
*.p12
*.pfx
**/*secret*
**/*key*
**/*token*
**/*password*
!packages/recall-forge/extraction/secret-redactor.ts
.cache_ggshield
*.backup

# Development artifacts
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/settings.json
.idea/

# Build outputs
dist/
out/
build/
coverage/
.nyc_output/
.next/

# Package artifacts  
*.tgz
*.vsix.backup
*.tgz.backup
*.tsbuildinfo
.kiro/settings/mcp.json
test-github-mcp.cjs
mcp-config-fixed.json

# Package lock files (using bun.lock instead)
package-lock.json
**/package-lock.json

# Local MCP server directory
mcp-server/

# Local IDE/agent settings (per-machine, not shared)
.claude/settings.local.json
.cursor/settings.local.json
.windsurf/settings.local.json

# Pi taskplane runtime state — per-developer, regenerates from base templates.
# We commit project rules in .pi/agents/supervisor.md (shared) but exclude
# audit logs, metrics, and the local migrations ledger.
.pi/teams/
.pi/taskplane.json
# cli/ subpackage has no project-specific Pi rules yet (template-only supervisor).
# Ignore the whole tree; un-ignore (or `git add -f`) when project rules are added.
cli/.pi/

testsprite_tests/tmp/config.json
