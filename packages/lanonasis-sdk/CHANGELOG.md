# Changelog - @lanonasis/sdk

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Changed
- **Repository metadata corrected (PR-A.2).** Added missing `directory` field
  (`packages/lanonasis-sdk`) and normalised URL to
  `git+https://github.com/lanonasis/lanonasis-maas.git`. Homepage re-pointed
  at the canonical `tree/main/packages/lanonasis-sdk#readme` URL.
- Engines requirement normalised: `node >= 18`.
- Files allowlist tightened: dropped redundant `package.json` entry, added
  `LICENSE`.

## [0.0.0]

Placeholder for releases — this package has not been published to npm yet.
First-publish is owner action (out of scope for PR-A.2; see blueprint §6.2).
