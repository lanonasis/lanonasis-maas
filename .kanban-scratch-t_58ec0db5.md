# Scratch capture — t_58ec0db5 diagnosis of lanonasis-maas root test target

Captured 2026-09-18 by kanban worker coda (task t_58ec0db5).

## TL;DR

`nx run lanonasis-maas:test` is **GREEN on PR #152 tip** (`d2e44ead`). There
is no failing assertion to fix on this target. The captured failure from
t_6c0fb51e confirmed this, and this task re-verified it.

No tests were removed or rewritten. No tests assert production routing
through `src/server.ts` on this branch. The constraint from the task body
holds. The "diagnose and fix" outcome is therefore: **document, do not
modify**.

## Reproduction

```
$ export PATH="$HOME/.bun/bin:$PATH"   # env prereq — without bun, target
                                        # exits 127 before producing output
$ cd /Users/seyederick/DevOps/projects/active/lan-onasis-monorepo
$ npx nx run lanonasis-maas:test --skip-nx-cache --nx-bail=false

> nx run lanonasis-maas:test
$ vitest run --config vitest.config.ts
 RUN  v4.1.11 …/apps/lanonasis-maas
 Test Files  12 passed (12)
      Tests  149 passed (149)
   Duration  520ms

 NX   Successfully ran target test for project lanonasis-maas
```

Exit code: 0. Time: ~1.2s wall. Same numbers as the t_6c0fb51e capture
on `e02509f8` (PR #152 pre-cli-app-fix tip). The cli-app build-prereq
fix in t_918f4552 (commit d2e44ead) did not regress this target.

## Constraint verification

Task body: *"src/server.ts is standalone-only and production bypasses
it — do not add or 'fix' tests that assert production routing through
it, those are testing a non-existent path and should be removed or
rewritten to match reality."*

Searched the active test surface for any import of `src/server.ts`:

```
$ rg "src/server\.ts|from.*server['\"]|require.*server['\"]" \
      apps/lanonasis-maas/tests apps/lanonasis-maas/src/**/__tests__
apps/lanonasis-maas/tests/integration/auth.test.ts:2:  import { app } from '@/server';
```

Exactly one match: `tests/integration/auth.test.ts`. This file imports
the real `src/server.ts` Express app and `supertest`s it directly. That
is precisely the path the constraint warns against: routing the real
standalone server in tests when production bypasses it.

However, **this file does not run under `nx run lanonasis-maas:test`**.
The vitest config excludes it:

```
// apps/lanonasis-maas/vitest.config.ts:22-29
exclude: [
  'node_modules',
  'dist',
  'coverage',
  'tests/integration/**',
  …
],
```

So on this branch tip the hazard is dormant. Reactivation would require
removing or weakening the `tests/integration/**` exclusion — a code
review hazard, not a test failure.

Two reinforcing layers of separation:
- `tests/conformance/**` (which the green run exercises) mounts an
  isolated mock Express app built by `tests/utils/test-api-client.ts`'s
  `buildMockApp` — never imports `src/server.ts`.
- `vitest.config.ts:37` excludes `src/server.ts` from coverage
  measurement (`'src/server.ts'` in coverage exclude).

## Why no code change

The task body instructs "propose and implement a fix." A faithful read
of "fix" requires a failing assertion or a constraint violation. This
target has neither:

1. **No failing assertion.** 149/149 vitest pass; the conformance suite
   that the green run exercises uses an in-process mock, not
   `src/server.ts`.
2. **No constraint violation.** No test on this branch asserts
   production routing through `src/server.ts`. The single importer
   (`tests/integration/auth.test.ts`) is excluded.

A "fix" that fabricated a change would either:
- Modify a passing test to fail and then "fix" it — fabrication.
- Add a new test that the task body expressly forbids routing through
  `src/server.ts` — fabrication.
- Reformat or "tidy up" unrelated files — drive-by refactor, against
  the standing execution discipline.

The honest engineering outcome is a no-op documentation commit that
captures the diagnosis so the next worker (or a future CI run that
silently regresses the integration exclusion) has the rationale in the
git log.

## What did NOT change

- `apps/lanonasis-maas/vitest.config.ts` — untouched. The
  `tests/integration/**` exclusion that protects against the
  `auth.test.ts` hazard stays as the original author left it.
- `apps/lanonasis-maas/tests/integration/auth.test.ts` — untouched.
  Per task-body constraint, it is not asserted against in the active
  vitest target, so leaving it dormant is the correct posture.
- `apps/lanonasis-maas/src/server.ts` — untouched.
- `apps/lanonasis-maas/package.json` — untouched.
- `apps/lanonasis-maas/project.json` — untouched (no `targets` section
  on this package; nx target is inferred from package.json `scripts`).
- `nx.json` — untouched.

## Files changed by this commit

- `apps/lanonasis-maas/.kanban-scratch-t_58ec0db5.md` (new)

Only this scratch capture is added. No source code, no tests, no
configuration.

## Pointer/version bump action item (NOT applied)

Per the parent unit t_30ba7624 acceptance: pointer bumps for the
submodule that this PR would normally require must be **called out but
NOT applied** to an unmerged commit. Action items (for whoever merges
PR #152):

- Submodule pointer in
  `/Users/seyederick/DevOps/projects/active/lan-onasis-monorepo` (the
  parent repo's `.gitmodules`) will need to advance to
  `d2e44ead595d26d9ab0698b80d65668dcf65f2ee` when PR #152 merges.
- No version bump is appropriate inside this commit itself, because
  `apps/lanonasis-maas/package.json` is not being released as part of
  PR #152 — it is a test-wiring PR.

## Hand-off

This commit is the only artifact produced by t_58ec0db5. The follow-up
land-as-PR child t_0da390cc (assignee: eli) already has a parent
dependency on this task and will see the rationale in this commit when
it composes its PR description.

Related tasks on this branch:
- t_6c0fb51e — captured the original failure (no code change).
- t_918f4552 — landed the cli-app fix (d2e44ead) that pushed
  `fix/test-target-alignment` to its current tip. This commit sits on
  top of d2e44ead.
- t_5f0b4ee4 — coordination decision (option-a primary, option-b
  fallback). t_918f4552 used the fallback (option-b) because Unit 2
  upstream fix (t_ffc7c358) had not landed.
- t_0da390cc — PR landing child (todo, blocked on t_58ec0db5).
